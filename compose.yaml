services:
  gateway-app:
    build:
      context: .
    env_file:
      - ./src/.env
    ports:
      - "4000:4000"
    volumes:
      - ./data:/app/data
      - ./dummy-mail:/app/dummy-mail
    environment:
      NODE_ENV: production
    restart: unless-stopped